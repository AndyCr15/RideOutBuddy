<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:title="Sort Groups By">
        <menu>
            <item
                android:id="@+id/nav_atoz"
                android:icon="@drawable/atoz"
                android:title="A to Z" />
            <item
                android:id="@+id/nav_ztoa"
                android:icon="@drawable/ztoa"
                android:title="Z to A" />
            <item
                android:id="@+id/nav_time"
                android:icon="@drawable/timer"
                android:title="Recently Used" />
        </menu>
    </item>

    <item android:title="Options">
        <menu>
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/logout"
                android:title="Change User" />
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/settings"
                android:title="Settings" />
            <item
                android:id="@+id/nav_exit"
                android:icon="@drawable/exit"
                android:title="Exit App" />
        </menu>
    </item>
</menu>